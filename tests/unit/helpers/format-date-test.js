import {
  formatDate
} from 'yesnopuzzle/helpers/format-date';

module('FormattedDateHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = formatDate(42);
  ok(result);
});
